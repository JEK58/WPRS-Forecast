import { updateForecastAccuracy } from "@/utils/update-forecast-accuracy";

describe("Update forecast accuracy", () => {
  it("", async () => {
    const res = await updateForecastAccuracy();
  }, 200000);
});

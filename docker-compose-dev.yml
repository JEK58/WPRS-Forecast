version: '3.8'
services:
  redis:
    image: redis:6-alpine3.17
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 60 1 --loglevel warning 
    volumes: 
      - cache:/data
volumes:
  cache:
    driver: local